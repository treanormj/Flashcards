<h1><%= @topic.topic %></h1>
<div class=”masonry-layout”
  <div class=”masonry-layout-panel”>
    <div class=”masonry-layout-panel-content”>
      <% @flashcards.each do |card| %>
      <%= link_to(image_tag(card.picture_url), topic_flashcard_path(card))%>
      <% end %>
    </div>
  </div>
</div>
</br>
<div class=”masonry-layout”
  <div class=”masonry-layout-panel”>
    <div class=”masonry-layout-panel-content”>
      <%= button_to "Home", root_path, class:"home-link", method: :get %>
      <%= button_to "Link to Google Translate", "https://translate.google.com/", class: "translate", method: :get %>
</div>
</div>
</div>
